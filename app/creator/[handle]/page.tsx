// @ts-nocheck
"use client";

import { ArrowLeft, ExternalLink, Mail, Pencil, Share2 } from "lucide-react";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { usePathname } from "next/navigation";

type Product={id:number;slug:string;title:string;description:string;type:string;created_at:string;likes:number;favorites:number;views:number;media:{object_key:string;media_type:string;mime_type:string}[]};
type Data={profile:{displayName:string;handle:string;bio:string;avatarUrl:string|null;contactEmail:string|null;isOwner:boolean};products:Product[];redirectHandle?:string};

export default function CreatorPublicPage(){const pathname=usePathname(),handle=decodeURIComponent(pathname.split("/").filter(Boolean).pop()||"");const [data,setData]=useState<Data|null>(null),[error,setError]=useState(""),[sort,setSort]=useState<"new"|"popular">("new"),[type,setType]=useState("全部");useEffect(()=>{fetch(`/api/creator/public?handle=${encodeURIComponent(handle)}`).then(async response=>{const result=await response.json();if(!response.ok)throw Error(result.error||"主页暂时无法打开");if(result.redirectHandle){location.replace(`/creator/${encodeURIComponent(result.redirectHandle)}`);return null}return result}).then(result=>result&&setData(result)).catch(cause=>setError(cause instanceof Error?cause.message:"主页暂时无法打开"))},[handle]);const types=useMemo(()=>["全部",...Array.from(new Set((data?.products||[]).map(product=>product.type)))],[data]);const products=useMemo(()=>{const items=(data?.products||[]).filter(product=>type==="全部"||product.type===type);return [...items].sort((a,b)=>sort==="popular"?(b.likes+b.favorites+b.views)-(a.likes+a.favorites+a.views):Date.parse(b.created_at)-Date.parse(a.created_at))},[data,sort,type]);if(!data)return <main className="grid min-h-dvh place-items-center bg-[#f7f7f4] px-5 text-center text-sm text-black/50">{error||"正在打开创作者主页…"}</main>;const {profile}=data;return <main className="min-h-dvh bg-[#f7f7f4] text-[#171717]"><header className="border-b border-black/10"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link href="/" className="inline-flex items-center gap-2 text-sm text-black/50"><ArrowLeft size={16}/>First Look</Link><div className="flex gap-2">{profile.isOwner&&<Link href="/account/creator" className="inline-flex items-center gap-2 rounded-full border border-black/15 bg-white px-4 py-2 text-sm"><Pencil size={14}/>编辑主页</Link>}<button onClick={()=>navigator.clipboard.writeText(location.href)} aria-label="复制主页链接" className="rounded-full border border-black/15 bg-white p-2.5"><Share2 size={15}/></button></div></div></header><section className="mx-auto max-w-6xl px-5 pb-20 pt-12"><div className="grid gap-8 border-b border-black/15 pb-12 md:grid-cols-[160px_1fr_auto] md:items-end"><div className="grid size-32 place-items-center overflow-hidden rounded-full bg-black text-4xl font-semibold text-white md:size-40">{profile.avatarUrl?<img src={profile.avatarUrl} alt={`${profile.displayName}的头像`} className="h-full w-full object-cover"/>:profile.displayName.slice(0,1).toUpperCase()}</div><div><p className="text-sm text-black/40">@{profile.handle}</p><h1 className="mt-2 text-5xl font-semibold tracking-[-.07em] md:text-6xl">{profile.displayName}</h1><p className="mt-5 max-w-2xl whitespace-pre-wrap text-sm leading-7 text-black/60">{profile.bio||"这位创作者还没有写介绍。"}</p></div>{profile.contactEmail&&<a href={`mailto:${profile.contactEmail}`} className="inline-flex w-fit items-center gap-2 rounded-full bg-black px-5 py-3 text-sm text-white"><Mail size={15}/>联系创作者</a>}</div><div className="mt-10 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm text-black/40">公开产品</p><h2 className="mt-1 text-3xl font-semibold tracking-[-.055em]">{data.products.length} 件作品</h2></div><div className="flex flex-wrap gap-2"><select value={type} onChange={event=>setType(event.target.value)} className="rounded-full border border-black/15 bg-white px-4 py-2 text-sm">{types.map(item=><option key={item}>{item}</option>)}</select><button onClick={()=>setSort(sort==="new"?"popular":"new")} className="rounded-full border border-black/15 bg-white px-4 py-2 text-sm">{sort==="new"?"最新发布":"最受关注"}</button></div></div>{products.length?<div className="mt-8 grid gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{products.map(product=>{const media=product.media[0];return <Link key={product.id} href={`/work/${encodeURIComponent(product.slug)}`} className="group"><div className="aspect-[4/3] overflow-hidden bg-black/[.06]">{media&&(media.media_type==="image"||media.mime_type.startsWith("image/"))?<img src={`/api/media/file?key=${encodeURIComponent(media.object_key)}`} alt="" className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]"/>:media?<div className="grid h-full place-items-center bg-black text-sm text-white/60">视频作品</div>:<div className="grid h-full place-items-center text-sm text-black/30">暂无封面</div>}</div><div className="mt-4 flex items-start justify-between gap-4"><div><h3 className="text-xl font-semibold tracking-[-.04em]">{product.title}</h3><p className="mt-1 line-clamp-2 text-sm leading-6 text-black/50">{product.description}</p></div><ExternalLink size={16} className="mt-1 shrink-0 text-black/30"/></div><p className="mt-3 text-xs text-black/35">{product.type} · {product.likes} 喜欢 · {product.views} 浏览</p></Link>})}</div>:<div className="mt-10 border border-dashed border-black/15 py-16 text-center text-sm text-black/45">这里还没有公开产品。</div>}</section></main>}
